streamingCopies:
  baseDir: /data/av-migration/springfield
  sourceCsv: /data/av-migration/sources.csv

stagingDir: ${HOME}/staging

#
# See https://www.dropwizard.io/en/latest/manual/configuration.html#logging
#
logging:
  level: INFO
  appenders:
    - type: file
      archive: false
      timeZone: system
      currentLogFilename: ${HOME}/easy-preprocess-av-bag.log
